spring:
  application:
    name: nacos-discovery-consumer

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        username: nacos
        password: nacos

server:
  port: 9090